<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ctxt="clr-namespace:ColorTextBlock.Avalonia;assembly=ColorTextBlock.Avalonia">
    <Style Selector="ctxt|CTextBlock.Heading1">
        <Setter Property="FontSize" Value="30" />
        <Setter Property="Margin" Value="0,0,15,0" />
        <Setter Property="FontWeight" Value="UltraBold" />
        <Setter Property="Foreground" Value="{DynamicResource SukiText}" />
    </Style>
    <Style Selector="ctxt|CTextBlock.Heading2">
        <Setter Property="Margin" Value="5,10,15,10" />
        <Setter Property="FontSize" Value="24" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource SukiText}" />
    </Style>
    <Style Selector="ctxt|CTextBlock.Heading3">
        <Setter Property="Margin" Value="5,10,10,10" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource SukiText}" />
    </Style>
    <Style Selector="ctxt|CTextBlock.Heading4">
        <Setter Property="Margin" Value="5,10,5,10" />
        <Setter Property="FontSize" Value="17" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource SukiText}" />
    </Style>
    <Style Selector="ctxt|CTextBlock.Heading5">
        <Setter Property="FontSize" Value="15" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource SukiText}" />
    </Style>
    <Style Selector="ctxt|CTextBlock.Heading6">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource SukiText}" />
    </Style>
    
    <!-- ~1~ GitHub-style Alert Blocks @1@ -->
    <!-- ~1~ Note Alert (Blue) @1@ -->
    <!-- <Style Selector="Border.AlertNote"> -->
    <!--     <Setter Property="Background" Value="#ddf4ff" /> -->
    <!-- </Style> -->
    <!-- -->
    <!-- ~1~ Tip Alert (Green) @1@ -->
    <!-- <Style Selector="Border.AlertTip"> -->
    <!--     <Setter Property="Background" Value="#d1f7c4" /> -->
    <!-- </Style> -->
    <!-- -->
    <!-- ~1~ Important Alert (Purple) @1@ -->
    <!-- <Style Selector="Border.AlertImportant"> -->
    <!--     <Setter Property="Background" Value="#fbefff" /> -->
    <!-- </Style> -->
    <!-- -->
    <!-- ~1~ Warning Alert (Yellow) @1@ -->
    <!-- <Style Selector="Border.AlertWarning"> -->
    <!--     <Setter Property="Background" Value="#fff8c5" /> -->
    <!-- </Style> -->
    <!-- -->
    <!-- ~1~ Caution Alert (Red) @1@ -->
    <!-- <Style Selector="Border.AlertCaution"> -->
    <!--     <Setter Property="Background" Value="#ffebe9" /> -->
    <!-- </Style> -->
    
    <!-- <Style Selector="ctxt|CCode.CodeBlock"> -->
    <!--     <Style.Setters> -->
    <!--         <Setter Property="MonospaceFontFamily" Value="Consolas, Microsoft YaHei, Global Monospace" /> -->
    <!--         <Setter Property="Padding" Value="20,10" /> -->
    <!--         <Setter Property="MonospaceFontFamily" Value="Meiryo" /> -->
    <!--         <Setter Property="Foreground" Value="{DynamicResource SukiLowText}" /> -->
    <!--         <Setter Property="Background" Value="{DynamicResource SukiBackground}" /> -->
    <!--     </Style.Setters> -->
    <!-- </Style> -->
</Styles>
